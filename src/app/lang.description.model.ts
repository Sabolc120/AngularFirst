export class langDescription{
    public title: string;
    public inventor: string;
    public description: string;

    constructor(title: string, inventor: string, description:string){
        this.title = title;
        this.inventor = inventor;
        this.description = description;
    }
}